using VContainer;
using VContainer.Unity;

namespace Anycrat.Core
{
    public sealed class GlobalLifeTimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}